---
import PageIntro from '../../components/PageIntro.astro';
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getAllPublications, getAllTags } from '../../lib/content';
import { displayTag } from '../../lib/publications';

const entries = await getAllPublications();
const tags = getAllTags(entries);
---

<BaseLayout
  title="Tags"
  description="Browse After the Diagram posts and white papers by topic."
>
  <div class="wide-shell">
    <PageIntro
      eyebrow="One publication, two formats"
      title="Tags"
      description="Topics connect field notes with papers that develop the same platform, economics, and delivery questions in depth."
    />
    <div class="tags-cloud">
      {
        [...tags.entries()].map(([tag, count]) => (
          <a class="tag-card" href={`/tags/${tag}/`}>
            <span>{displayTag(tag)}</span>
            <span>
              {count} {count === 1 ? 'entry' : 'entries'}
            </span>
          </a>
        ))
      }
    </div>
  </div>
</BaseLayout>
